environment:
  matrix:
    - nodejs_version: "6"
    - nodejs_version: "8"

platform:
  - x64
  - x86

build: off

cache:
 - "%LOCALAPPDATA%\\Yarn"

install:
  - ps: Install-Product node $env:nodejs_version $env:platform
  - npm i

test_script:
  - node --version
  - npm run test:coverage
